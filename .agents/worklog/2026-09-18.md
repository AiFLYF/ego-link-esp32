# 工作日志 2026-09-18

## 09:32 · 既有提交（事故前）
- `937a815 fix(server)`：服务端大模型超时解耦（后台线程 + pending）、统计窗口修正、JSONL 落盘持久化。

## 10:10 · trae（Git Flow 骨架第一版）
- 远程尚无 develop；从本地 main（含 937a815）创建并推送 develop。
- 切 `feature/fix-gitattributes-text`，提交 .gitattributes 规则修复
  （`* binary` 原写在文本白名单之后，反向覆盖 text/diff，导致全部代码被当二进制）；
  dependencies.lock 随 renormalize 由 CRLF→LF（忽略 CR 零差异）。
- `python tools/verify_server.py` 27/27 通过。

## 10:06–10:30 · 事故：并行会话互踩（详见 AGENTS.md 第 5 节）
- 另一会话删除旧 `.git`、重建远程为 4 个临时 hash 的历史、重新 git init
  （unborn 分支 feature/device-batched-telemetry）；本会话 stash 的设备端 100Hz 改动随之丢失。
- 排查：回收站里的 .git 是空壳；Trae Local History 仅有 .gitattributes；GitHub 旧 commit 一度不可达。
- 新版 server.py 在工作区幸存，备份于仓库外 `../_salvage/server.py.937a815-recovered`。

## 10:40 · trae（恢复）
- 用户确认另一会话系本人安排并授权接手；远程随后恢复原始 6 commit 历史（main=develop=937a815）。
- 经用户明确授权后 `reset --hard` 对齐本地 main/develop 到 origin；server.py 与历史版本 hash 校验一致。
- 重做 .gitattributes 修复（commit ec95b33），27/27 通过，推送并创建
  PR #1：<https://github.com/AiFLYF/ego-link-esp32/pull/1>（base=develop）。
- 从事故前会话记录恢复 3 个设备端文件（transport.h / Kconfig.projbuild / idf_component.yml，
  与原 blob 逐字节一致），本地提交于 feature/device-batched-telemetry（3cd8357，未推送，
  缺实现文件固件不可编译）。

## 11:15 · trae（多 agent 协作体系）
- 建立 AGENTS.md（Git Flow + 共享记忆协议权威总章）、`.agents/`（sessions/worklog/inbox）、
  `tools/agents_status.py`（跨远程分支聚合看板）、Claude Code 的 CLAUDE.md 与 /git-flow 命令、
  WorkBuddy 本地镜像入口。
- 触发原因即本次事故：开工前无任何机制能发现另一会话正在操作 .git。

## 11:24 · trae（发现设备端工作已在本地完成）
- buddy 会话的 `feature/device-batched-telemetry` 已有 a57da4d（100Hz 批量遥测 + 服务端配套），
  基于 937a815；**尚未推送、无 PR**，工作区剩 dependencies.lock 未提交。
- 为避免切换分支影响其工作区，trae 改用 `git worktree add ../_flow-wt` 从 origin/develop
  切 docs 分支落盘协作体系文件（worktree 方案后因沙箱限制改用主工作区快速切换）。
  inbox Q-2026-09-18-01 已转请 buddy 推送并开 PR。
- 本地提交 191163c（docs 分支）；当日 GitHub 网络中断，未能推送。

## 11:36 · trae（发现与 buddy 的设计撞车，停手请示）
- buddy 活跃期间给 .gitignore 加了 `.agent/`（单数）与 `.claude/`：他也在规划本地 agent 目录。
- 用户决策（09-19 02:5x 确认）：`.claude/`、`.agent/`、`.workbuddy-ai/` 全部本地私有不入库；
  共享记忆固定用 `.agents/`（复数，入库）；Claude 斜杠命令通过入库的
  docs/agents/claude-git-flow-command.md 分发、本地复制安装。
