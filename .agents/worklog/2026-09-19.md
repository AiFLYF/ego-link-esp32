# 工作日志 2026-09-19

## 02:55 · trae（协作体系按用户决策定稿）
- 确认远程：PR #1 仍 OPEN（base=develop，未合并）；buddy 的 device 分支仍未推送。
- 用户拍板：`.claude/`、`.agent/`、`.workbuddy-ai/` 三类目录**只在本地、不入 git**；
  团队共享只有 `.agents/`（复数）与入库的 `CLAUDE.md`、`AGENTS.md`。
- 据此修订 191163c：
  - 从提交移除 `.claude/commands/git-flow.md`；命令文件改存入库的
    `docs/agents/claude-git-flow-command.md`（含一键本地复制安装说明）。
  - .gitignore 采纳 buddy 工作区新增的 `.agent/`、`.claude/` 两行（dependencies.lock
    是 buddy 的设备改动，未纳入本提交，继续留在其工作区）。
  - AGENTS.md 第 4 节补 `.agents/` vs `.agent/` 命名辨析，第 7 节改本地安装说明。
- 本地私有安装（不入库）：`.claude/commands/git-flow.md`、`.workbuddy-ai/git-flow-agent.md` 镜像。
- 门禁：agents_status.py --selfcheck PASS；verify_server.py 27/27。
- commit 93834bf 推送，**PR #2** <https://github.com/AiFLYF/ego-link-esp32/pull/2>（base=develop）。
  会话置 done，等待合并。与 PR #1 文件无交集。
